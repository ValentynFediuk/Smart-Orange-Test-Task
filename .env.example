# Example environment configuration
# Copy or rename this file to ".env" and adjust values as needed.
# The webpack config will load .env and inject variables into the client bundle via DefinePlugin.

# API endpoint for portfolio data (JSON)
API_URL=https://test.smarto.agency/smarto_complexes_list.json

# Number of items to show initially and on each "Load More" click
PAGE_SIZE=3
